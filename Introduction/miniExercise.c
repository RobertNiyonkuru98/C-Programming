#include <stdio.h>

int main(void) {
  int employeeId = 1;
  int salaryScale = 400;
  int salary = 400;
  int dependents = 2; // Assigned a value to dependents

  // Print all the information
  printf("Employee ID: %d\n", employeeId);
  printf("Salary Scale: %d\n", salaryScale);
  printf("Salary: %d\n", salary);
  printf("Dependents: %d\n", dependents);

  return 0;
}